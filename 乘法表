#include <stdio.h>
#include <windows.h>
int main()
{
	int i, k, a;
	for (i = 1; i <= 9; i++)
	{
		for (k = 1; k <= i; k++)
		{
			a = k * i;
			printf("%dx%d=%d   ", k, i, a);
		}
		printf("\n");
	}
}
